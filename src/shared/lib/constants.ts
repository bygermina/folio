export const EMPTY_ARRAY: readonly never[] = Object.freeze([]) as readonly never[];

export const EMPTY_OBJECT: Readonly<Record<string, never>> = Object.freeze({}) as Readonly<
  Record<string, never>
>;


